"""Shared model resources for Layer 2 (the resource axis).

Each engine wraps one model/package, is built once per run, and is shared by any
extractors that need it. This is where grouping by Python package belongs.
"""
