# Observation source inputs (large binaries, transient per PROJECT.md; retrievable by CID)
input/
*.mp4

# Per-video pipeline data (observations, events, run manifests): regenerable from
# inputs, video-specific, not part of the shared repo. One subdir per source_id.
data/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.pytest_cache/
.venv/

# Claude Code (local agent config, not part of the shared repo)
.claude/

# Project/agent instructions (kept out of the shared repo)
CLAUDE.md
PROJECT.md

# OS / editor
.DS_Store
Thumbs.db
